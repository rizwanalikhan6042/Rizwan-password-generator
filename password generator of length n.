// maximum length of password should be 4.
#include <bits/stdc++.h>
  using namespace std;

  void passwordGen(int x,int n,int y,int i){
  //base condition
    if(i>y)
    return ;
    //for password of length 2.
    if(n==2){
      if(x%10==9){
        cout<<x<<'\n';
      passwordGen(x+2,n,y,i+2);
       }
       else{
         cout<<x<<'\n';
         passwordGen(x+1,n,y,i+1);
      }
    }
    //for password of length 3.
     else if(n==3)  {
       if(x%10==9&&x%100==99){
          cout<<x<<'\n';
          passwordGen(x+12,n,y,i+12);
      
       }
       else if(x%10==9){
         cout<<x<<'\n';
          ocd(x+2,n,y,i+2);
         
       }
       else{
       cout<<x<<'\n';
         ocd(x+1,n,y,i+1);}
     }
     //for password of length 4.
     else if(n==4){
       if(x%10==9&&x%100==99&&x%1000==999){
          cout<<x<<'\n';
          passwordGen(x+112,n,y,i+112);
      
       }
       else if(x%10==9&&x%100==99){
         cout<<x<<'\n';
          passwordGen(x+12,n,y,i+12);
       }
       else if(x%10==9){
          cout<<x<<'\n';
          passwordGen(x+2,n,y,i+2);
       }
       else{
         cout<<x<<'\n';
       passwordGen(x+1,n,y,i+1);
       
       }
     }
    
   
  }
  int main()
  {
    //write your code here
    // t is for number of test cases
    int t;
    cin>>t;
    while(t--){
    //n is for length of password.
      int n,x=1,o=10,i;
      cin>>n;
     
      for(int i=0;i<n-1;i++){
      x=10*x+1;
      o=o*10;
     
      }
     
      int y=o-x;
     // x could be 10 for password of length 2,100 for password of length 3,1000 for password of length 4. y is for base condition of recursion or maximum value of i.
     // i is just adding element for recursion. initially its value should be 1.
     passwordGen(x,n,y,1);
    }
    return 0;
  }
